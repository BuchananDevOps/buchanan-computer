import { FC } from "react"

type Props = {
  heading?: string
  subHeading?: string
  description?: string
  className?: string
}

const Testimonial: FC<Props> = ({
  heading,
  subHeading,
  description,
  className,
}) => {
  return <></>
}

export default Testimonial
